/******************************************************************************/

export default function styles() {
  const full = {
    margin: '50px',
    padding: '50px',
    flexGrow: '1',
    display: 'flex',
    flexDirection: 'column',
    backgroundColor: 'white',
  };

  const button = {
    padding: '20px',
  };

  const header = {
    display: 'flex',
    flexDirection: 'row',
    flexGrow: '1',
  };

  return {
    full,
    button,
    header,
  };
}

/******************************************************************************/
