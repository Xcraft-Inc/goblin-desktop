/******************************************************************************/

export default function styles() {
  const full = {
    flexGrow: '1',
    display: 'flex',
    flexDirection: 'column',
    backgroundColor: 'white',
  };
  const header = {
    display: 'flex',
    flexDirection: 'row',
    flexGrow: '1',
  };

  return {
    full,
    header,
  };
}

/******************************************************************************/
